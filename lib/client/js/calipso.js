/**
 * Calipso client side JS library
 *
 * This library is included in every page request, and should be used to manage all client side
 * Javascript libraries.
 *
 */
var Calipso = function () {
};

/**
 * Require a library
 */
Calipso.prototype.require = function (jsFile) {

}
